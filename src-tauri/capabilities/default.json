{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissões da janela principal. O Tauri v2 nega tudo por padrão: cada capacidade nova entra aqui, explicitamente.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
